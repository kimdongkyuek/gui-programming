// ==========================================================================
//  model.h - 데이터(모델) 부분의 "사용 설명서"
// --------------------------------------------------------------------------
//  헤더 파일에는 "무엇을 할 수 있는지"(함수 이름과 모양)만 적습니다.
//  실제로 "어떻게 하는지"는 model.cpp 에 있습니다.
//  W02Win32.cpp 는 이 헤더만 include 하면 점을 저장하고 꺼내 쓸 수 있습니다.
//  -> 나중에 저장 방식을 배열에서 다른 것으로 바꿔도 W02Win32.cpp 는 안 고쳐도 됩니다.
//     이것이 "모델을 분리한다"는 말의 뜻입니다.
// ==========================================================================
#pragma once            // 이 헤더가 두 번 이상 포함되는 것을 막아 줍니다.
#include "framework.h"  // POINT 구조체를 쓰기 위해 필요합니다.

#define MODEL_MAX_POINTS	3   // 최대 몇 개까지 저장할지. 숫자를 늘리면 더 많이 그릴 수 있습니다.

int GetNumPoints();         // 지금까지 저장된 점의 개수
POINT GetPoint(int);        // i 번째 점 꺼내기

void AddPoint(POINT);       // 점 하나 추가 (꽉 찼으면 무시)
void DeleteLastPoint();     // 마지막 점 하나 삭제 (Undo)
